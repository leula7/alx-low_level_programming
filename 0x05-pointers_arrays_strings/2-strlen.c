#include "main.h"
#include <string.h>

/**
 * _strlen - to count length.
 * @s: The given character.
 * @str_length: The initializer.
 */
int _strlen(char *s)
{
int str_length;
str_length = strlen(s);
return str_length;  
}
