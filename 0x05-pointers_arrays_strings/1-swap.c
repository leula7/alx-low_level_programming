#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
  /**
 * main - check the code
 *
 * Return: Always 0.
 */
{
int temp;
temp = *a;
*a = *b;
*b = temp;
}
